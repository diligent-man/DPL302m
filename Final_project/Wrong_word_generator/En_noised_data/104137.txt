If the word ends in -e remove the -e and add -li eg|If the word ends in -e remove the -e and add -li e
If the word ends in -e remove the -e and add -li eg|If the word ends in -e rvmoee the -e and add -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends in -e remove the -e nd add -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends in -e remove the -e and add li eg
If the word ends in -e remove the -e and add -li eg|If the word neds in -e remove the -e and add -li eg
If the word ends in -e remove the -e and add -li eg|If tmhe word ends in -e remove tmhe -e and add -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends i -e remove the -e and add -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends in -pe remove the -pe and add -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends in -e remove the -e and amdd -li eg
If the word ends in -e remove the -e and add -li eg|If the word ends in -ve remove the -ve and add -li eg
