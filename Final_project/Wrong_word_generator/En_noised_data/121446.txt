The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact solutions but rather apprximations and so they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact solutions but rather approximations and so thmey would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact olutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact oslutions but rather approximations and os they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact solutions but rather approximations and so thy would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|ehT Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were ot interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact solutions but rather approximations aynd so they would commonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact solutions but rather approximations and so they would cyommonly use linear interpolation to approximate intermediate values
The Babylonians were not interested in exact solutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values|The Babylonians were not interested in exact slutions but rather approximations and so they would commonly use linear interpolation to approximate intermediate values
