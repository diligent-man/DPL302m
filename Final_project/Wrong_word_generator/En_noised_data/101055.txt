is the set of all gates of depth|is the set of all gatejs of depth
is the set of all gates of depth|is the set of all gates of degpth
is the set of all gates of depth|is the saet of all gates of depth
is the set of all gates of depth|is the set of lal gates of depth
is the set of all gates of depth|is the set of eall gates of depth
is the set of all gates of depth|is the ste of all gates of depth
is the set of all gates of depth|si the set of all gates of depth
is the set of all gates of depth|s the set of all gates of depth
is the set of all gates of depth|s the set of all gates of depth
is the set of all gates of depth|is the set of all ates of depth
