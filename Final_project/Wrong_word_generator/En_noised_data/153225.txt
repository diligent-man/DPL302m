specifies the point where the line crosses the|specifies hte point where hte line crosses hte
specifies the point where the line crosses the|specifies the point were the line crosses the
specifies the point where the line crosses the|specifies the point wherqe the line crosses the
specifies the point where the line crosses the|specifies the opint where the line crosses the
specifies the point where the line crosses the|specifies the ponit where the line crosses the
specifies the point where the line crosses the|specifies th point where th line crosses th
specifies the point where the line crosses the|specifies the pint where the line crosses the
specifies the point where the line crosses the|specifies teh point where teh line crosses teh
specifies the point where the line crosses the|specifies th point where th line crosses th
specifies the point where the line crosses the|specifies hte point where hte line crosses hte
