For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|Fro an observation from the discrete component the likelihood function for an observation from the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discret component the likelihood function for an observation from the discret component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|rFor an observation from the discrete component the likelihood function for an observation from the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation rom the discrete component the likelihood function for an observation rom the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discrete component the likelihood function for an observation from the discrete component is sihmply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discrete conmponent the likelihood function for an observation from the discrete conmponent is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discrete component the likelihood function fr an observation from the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discrete component the likelihood function fr an observation from the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the discrete component the likelihood functioln for an observation from the discrete component is simply
For an observation from the discrete component the likelihood function for an observation from the discrete component is simply|For an observation from the dsicrete component the likelihood function for an observation from the dsicrete component si simply
