So for example if I say You shouldn't be so defensive|So for example i I say You shouldn't be so defensive
So for example if I say You shouldn't be so defensive|So for example if I say Yo shouldn't be so defensive
So for example if I say You shouldn't be so defensive|So for example if I say You shouldn't be so deiensfve
So for example if I say You shouldn't be so defensive|So for example if I say oYu shouldn't be so defensive
So for example if I say You shouldn't be so defensive|So ofr example if I say You shouldn't be so defensive
So for example if I say You shouldn't be so defensive|So for example f I say You shouldn't be so defensive
So for example if I say You shouldn't be so defensive|oS for example if I say You shouldn't be so defensive
So for example if I say You shouldn't be so defensive|So for example if I say You shotldn'u be so defensive
So for example if I say You shouldn't be so defensive|So for example if I say You shouldn't be mso defensive
So for example if I say You shouldn't be so defensive|S for example if I say You shouldn't be so defensive
