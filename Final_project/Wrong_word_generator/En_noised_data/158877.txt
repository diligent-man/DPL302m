bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table wdith the real color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table with thoe real color value specified in one of thoe othoer formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table with teh real color value specified in one of teh otehr formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table with the ral color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixl Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table with the ral color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bbits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an induex in a table with the real color value specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each valeu is an index in a table with the real color valeu specified in one of the other formats
bits per pixel Sometimes palette mode where each value is an index in a table with the real color value specified in one of the other formats|bits per pixel Sometimes palette mode where each value is an index in a table witch the real color value specified in one of the other formats
