can be chosen as the class of all finite subsets in|cna be chosen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|can be chosen as the class of lal finite subsets in
can be chosen as the class of all finite subsets in|can bpe chosen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|cn be chosen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|can b chosen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|can be chosen as the class of all einitf subsets in
can be chosen as the class of all finite subsets in|can be csohen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|can be chosen a the clas of all finite subsets in
can be chosen as the class of all finite subsets in|can b chosen as the class of all finite subsets in
can be chosen as the class of all finite subsets in|can be chosen as the class of all fnite subsets n
