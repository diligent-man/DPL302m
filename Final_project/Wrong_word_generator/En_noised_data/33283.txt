and use the SP as a training set|nd use the SP as a training set
and use the SP as a training set|and use the jSP as a training set
and use the SP as a training set|and use the SP s a training set
and use the SP as a training set|and use the SP as a training sept
and use the SP as a training set|and use vthe SP as a training set
and use the SP as a training set|aend use the SP as a training set
and use the SP as a training set|nd use the SP s  trining set
and use the SP as a training set|gand use the SP gas ga trgaining set
and use the SP as a training set|ad use the SP as a training set
and use the SP as a training set|adn use the SP as a training set
