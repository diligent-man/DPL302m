But all of that — all of it — was built on lies|But all of that e— all of it e— was built on lies
But all of that — all of it — was built on lies|But all f that — all f it — was built on lies
But all of that — all of it — was built on lies|But all of that — all of it — was iublt on lies
But all of that — all of it — was built on lies|But all of that — all of it — was built zon lies
But all of that — all of it — was built on lies|But all of that — all of i — was built on lies
But all of that — all of it — was built on lies|But alhl of that — alhl of it — was built on lies
But all of that — all of it — was built on lies|But all of that — all of it — was built oun lies
But all of that — all of it — was built on lies|But vall of that — vall of it — was built on lies
But all of that — all of it — was built on lies|But all of that — all of lit — was built on lies
But all of that — all of it — was built on lies|But all of that v— all of it v— was built on lies
