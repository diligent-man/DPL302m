maps a function to zero if and only if that function is constant|maps a function hto zero if and only if that function is constant
maps a function to zero if and only if that function is constant|aps a function to zero if and only if that function is constant
maps a function to zero if and only if that function is constant|maps a yfunction to zero if and only if that yfunction is constant
maps a function to zero if and only if that function is constant|maps a function two zero if and only if that function is constant
maps a function to zero if and only if that function is constant|maps a function to zero if and only if tat function is constant
maps a function to zero if and only if that function is constant|maps a nufction to zero if and only if that nufction is constant
maps a function to zero if and only if that function is constant|mas a function to zero if and only if that function is constant
maps a function to zero if and only if that function is constant|maps a function to zero if and only if thoat function is constant
maps a function to zero if and only if that function is constant|maps a functfion to zero if and only if that functfion is constant
maps a function to zero if and only if that function is constant|maps a function to zero if adn only if that function is constant
